<%= form_for(@challenge) do |f| %> 
  <% @rdio_search.search_results.each do |result| %> 
  <input type="radio" name= key value= <%= result.key %> > <%= result.name %> - <%= result.artist_name %> <br> 
  <%= f.hidden_field :song_name, :value => result.name %> 
  <% end %> 
  </select>
  <%= f.submit "Add Song to Challenge" %> 
<% end %> 